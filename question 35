int searchInsert(int* nums, int numsSize, int target) {
    // AFTER OBSERVING THE EXAMPLE 3
    /*firstly we will go through the loop to check is the number is greater or equal to 
    target if yes then target must be placed in side the loop 
    */
for(int i=0;i<numsSize;i++){
    if(nums[i]>=target)
    return i; // loop stops after this and the gives the ondex position of insert the target
}  
return numsSize; // the loop completly ends so , target will be placed at last index+1; i.e. numsSize
}
